name: provider_bank
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_native_splash: ^2.2.12
  flutter_launcher_icons: ^0.11.0
  google_fonts: ^3.0.1
  provider: ^6.0.4
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: 'images/catarina_icon.png'

flutter_native_splash:
  color: '#93cae8'
  image: 'images/catarina_logo.png'

flutter:
  uses-material-design: true
  assets:
    - images/