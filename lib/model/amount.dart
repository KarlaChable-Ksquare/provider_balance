class Amount {
  String title;
  String description;
  num amount;
  bool isIncExp;
  String createdTime;

  Amount({
    required this.title,
    required this.description,
    required this.amount,
    required this.isIncExp,
    required this.createdTime,
  });
}
